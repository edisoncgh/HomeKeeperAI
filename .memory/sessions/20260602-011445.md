# Session 20260602-011445

## Summary

用户要求按照 `CLAUDE.md` 开始 M3 第一个 slice 开发。本次按 project-memory、handoff-driven-development 和 TDD 推进 M3.1 LLM 配置与调用边界。

## Changes

- 新增 `lib/ai/config.ts`：读取并校验 `LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL`，归一化 Base URL，并允许本地 OpenAI 兼容服务无 API Key。
- 新增 `lib/ai/client.ts`：封装 OpenAI 兼容 `chat/completions` 调用，支持超时、有限重试、远端错误分类和脱敏 API 错误响应。
- 新增 `lib/api/ai.ts` 和 `app/api/ai/health/route.ts`：提供登录保护的 LLM 配置与连通性健康检查，不做识别业务。
- 新增 `tests/ai-config.test.ts`、`tests/ai-client.test.ts`、`tests/ai-api.test.ts`：覆盖配置、调用、重试、错误分类、认证、配置错误和密钥不泄露。
- 同步本地 `docs/TASKS.md`、`docs/API.md`、`docs/TECH_PLAN.md`、`docs/QUALITY.md`、`docs/CONTEXT.md`、`docs/AGENT_HANDOFF.md` 和项目记忆。

## Decision

M3.1 只提供 LLM 调用底座和健康检查接口。`GET /api/ai/health` 会真实调用配置的 OpenAI 兼容服务以验证连通性，但不会执行拍照识别、订单解析或主页 AI 建议。远端异常只返回中文提示和错误类型，不泄露 API Key 或原始远端错误。

## Verification

- `npm run test`：通过，14 个测试文件、41 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，`/api/ai/health` 为动态路由。
- `npm run db:check`：通过，SQLite 连接正常。

## Next

进入 M3.2：AI 结构化契约与解析校验。优先实现候选 JSON Schema、字段来源、置信度、多物品拆分、非 JSON 恢复和解析单元测试。不要直接实现拍照识别、订单解析或主页 AI 建议业务。
