# Session 20260602-130237

## Summary

完成 M3.4 拍照识别物品切片规划，更新 docs 和 `.memory/context.md`，等待用户确认后再编码。

## User Request

用户要求继续按照规范进行 M3.4 开发。

## Work Completed

- 重新加载 `project-memory`、`handoff-driven-development`、TDD 和 frontend-design 技能。
- 读取 `.memory/context.md`、`.memory/knowledge.md`、`.memory/index.md`。
- 读取 `docs/TASKS.md`、`docs/AGENT_HANDOFF.md`、`docs/API.md`、`docs/TECH_PLAN.md`、`docs/QUALITY.md`、`docs/DESIGN_SYSTEM.md`。
- 复核现有 AI 边界：`lib/ai/client.ts`、`lib/ai/prompts.ts`、`lib/api/ai.ts`。
- 更新 `docs/TASKS.md`：新增 M3.4 目标、范围、非目标、验收标准和建议实现切片。
- 更新 `docs/API.md`、`docs/TECH_PLAN.md`、`docs/QUALITY.md`、`docs/DESIGN_SYSTEM.md`、`docs/AGENT_HANDOFF.md`：同步 M3.4 计划边界。
- 更新 `.memory/context.md`：记录当前状态为 M3.4 已规划、等待确认。

## Planned M3.4 Slices

- M3.4.1 Vision client 与识别 API：扩展 AI client 支持 OpenAI 兼容 vision content array；新增 `POST /api/items/recognize`，完成认证、图片校验、Prompt、LLM 调用、解析和错误兜底。
- M3.4.2 `/items` 拍照识别入口：新增图片选择/拍照控件、补充说明、识别按钮、loading/错误状态，成功后复用 M3.3 候选确认面板。
- M3.4.3 运行时与人工验收收口：dev server 验证识别入口、候选确认、失败提示；同步 docs 和 `.memory`。

## Verification

本轮只做规划文档和项目记忆更新，未修改业务代码，未运行代码级验证。

## Notes

- M3.4 输入先采用浏览器端文件选择或拍照得到的 data URL；服务器不保存图片，不生成缩略图，不写入 `Item.imageUrl`。
- M3.4 不实现订单截图解析、主页 AI 建议、标签管理、批量导入或 M4 预警。
- 当前 M3.3 代码和项目记忆变更仍未提交；继续开发时不要回退这些变更。
- 本地旧加密 LLM API Key 仍可能需要用户在 `/settings` 中重新替换保存一次。

## Tags

#m3 #ai #photo-recognition #planning #handoff #memory
