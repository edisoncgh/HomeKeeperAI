# Session 20260603-203050

## Summary
用户指出 HomeStorageAssistant 的主要使用场景是母亲在手机端整理物品，因此拍照导入不能只是传图，还必须能在移动端直接唤起手机摄像头；PC 端可以不展示拍照入口。已将该需求规划为 M3.7 移动端拍照导入入口收口切片，暂缓进入 M4。

## Changes
- 更新 `docs/TASKS.md`：新增当前切片 M3.7 移动端拍照导入入口，明确目标、范围、非目标、验收标准和验证计划。
- 更新 `docs/AGENT_HANDOFF.md`：下一步从 M4 规划改为先确认并实现 M3.7。
- 更新 `.memory/context.md`、`.memory/index.md`。

## Plan
- 移动端显示明确“拍照导入”按钮，绑定带 `accept="image/*"` 和 `capture="environment"` 的隐藏 file input。
- 保留普通选图入口作为 fallback，继续复用现有 data URL -> `/api/items/recognize` -> 候选确认流程。
- PC 端不展示专门“拍照导入”入口。
- 不改后端识别 API、Prompt、parser、候选确认契约或图片持久化边界。

## Next
等待用户确认 M3.7 计划。确认后按 TDD 更新 `tests/ai-photo-recognition-ui.test.ts`，再实现 `components/ai/photo-recognition-panel.tsx`。

## Tags
`#m3` `#mobile` `#camera` `#photo-recognition` `#planning` `#memory`
