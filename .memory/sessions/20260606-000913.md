# Session 20260606-000913

## Summary

完成 M5.1 Dockerfile 与运行时镜像实现。用户确认切片后，新增 Dockerfile、.dockerignore、public/.gitkeep，并将 Next.js 配置切换到 standalone 输出；同步 docs 与 `.memory`，但 Docker daemon 未运行，镜像构建和容器启动仍需复验。

## Updated

- `Dockerfile`：新增多阶段构建，使用 `node:22.14.0-slim`，构建阶段执行 `npm ci`、`npx prisma generate`、`npm run build`，运行阶段复制 standalone 产物、静态资源和 Prisma 运行文件，使用非 root `node` 用户先执行 `node docker/init-db.mjs`，再执行 `node server.js`。
- `docker/init-db.mjs`：容器启动前用 Prisma Client 幂等执行现有 SQLite migration SQL，fresh SQLite volume 会先建表。
- `.dockerignore`：排除 `.env`、本地数据、上传/备份目录、缓存、docs、Agent 配置和 `.codegraph/`。
- `next.config.ts`：启用 `output: "standalone"`。
- `public/.gitkeep`：确保无静态资源时 Dockerfile 的 `COPY /app/public` 仍可执行。
- `docs/TASKS.md`、`docs/QUALITY.md`、`docs/AGENT_HANDOFF.md`、`Readme.md`：同步 M5.1 实现、验证结果和 Docker daemon 阻塞。
- `.memory/context.md`、`.memory/knowledge.md`、`.memory/index.md`：同步当前现场和长期知识。

## Current State

- M5.1 代码实现已完成。
- Docker/Linux 运行时不使用 `scripts/run-next.mjs` 的 wasm SWC/readlink patch，启动流程为 `node docker/init-db.mjs && node server.js`。
- Docker 镜像构建和容器启动仍未验收：当前环境 Docker CLI 存在，但 Docker Desktop Linux daemon 未运行。

## Verification

- `npm run lint` 通过，无错误输出。
- `npm run typecheck` 通过，无错误输出。
- `npm run test` 通过：34 个测试文件、113 个测试。
- `npm run build` 通过。
- `node --check docker/init-db.mjs` 通过。
- `DATABASE_URL=file:../data/m5-init-test.db node docker/init-db.mjs` 通过，首次创建 schema；重复运行保持幂等。
- `.next/standalone/server.js` 存在。
- `docker --version` 返回 Docker version 29.1.2。
- `docker build -t home-storage-assistant:m5.1 .` 未完成：无法连接 `dockerDesktopLinuxEngine`。

## Next Action

启动 Docker daemon 后复跑：

```bash
docker build -t home-storage-assistant:m5.1 .
docker run --rm -p 3000:3000 \
  -e DATABASE_URL=file:/app/data/home-storage.db \
  -e AUTH_SECRET=<至少32位高熵密钥> \
  -e PORT=3000 \
  -v <本机数据目录>:/app/data \
  home-storage-assistant:m5.1
```

访问 `/setup` 或 `/login` 完成人工验收。通过后再勾选 `docs/TASKS.md` 的 Docker 镜像构建和容器启动验收项，并进入 M5.2。

## Scope Guardrails

- M5.1 不写 Compose、不实现备份恢复、不改业务功能、不新增 UI polish。
- 不把 Docker 镜像构建或容器启动标记为完成，直到 Docker daemon 可用环境复验通过。
- `.codegraph/` 不提交。
