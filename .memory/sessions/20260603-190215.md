# Session 20260603-190215

## Summary
完成 M3.6 主页 AI 建议规划。切片定位为首页手动触发的只读 AI 建议闭环：服务端读取当前物品、分类和位置摘要，调用已配置 LLM，返回整理、补录、位置优化、消耗建议和信息核对建议；不写数据库，不生成 M4 预警，不后台自动调用。

## Updates
- 在 `docs/TASKS.md` 新增“规划中切片：M3.6 主页 AI 建议”，包含目标、范围、非目标、验收标准、建议实现切片和验证计划。
- 更新 `docs/AGENT_HANDOFF.md`，标记 M3.6 计划已写入 TASKS，等待用户确认后编码。
- 重写 `.memory/context.md`，记录当前状态为 M3.6 规划完成待确认。
- 更新 `.memory/index.md` 并新增本 session 记录。

## Planned Implementation
- M3.6.1 建议契约与解析。
- M3.6.2 建议 API：`POST /api/ai/dashboard-advice`。
- M3.6.3 首页总览与 AI 建议 UI。
- M3.6.4 运行时验证与文档收口。

## Next
等待用户确认 M3.6 计划。确认后按 TDD 从建议契约与解析开始编码。

## Tags
`#m3` `#ai` `#dashboard-advice` `#planning` `#handoff` `#memory`
