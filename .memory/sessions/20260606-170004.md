# Session 20260606-170004

## Summary

进入 M5.2 Docker Compose 与数据持久化切片，完成 Compose 配置实现、环境变量示例更新、静态配置校验、Compose 启动、SQLite Volume 持久化验证和文档/记忆同步。

## Trigger

用户确认按规划进入 M5.2，并要求继续遵守项目开发规范、项目开发记忆和 handoff 文档整理。

## Work Done

- 新增 `docker-compose.yml`：
  - 从当前 `Dockerfile` 构建镜像。
  - 支持 `IMAGE_TAG` 和 `NODE_IMAGE`。
  - 容器内固定 `DATABASE_URL=file:/app/data/home-storage.db`。
  - 容器内 `PORT=3000`，宿主端口使用 `${PORT:-3000}:3000`。
  - 挂载 `home-storage-data:/app/data`、`home-storage-uploads:/app/uploads`、`home-storage-backups:/app/backups`。
  - 使用 `${AUTH_SECRET:?...}` 要求生产密钥。
- 更新 `.env.example`，新增 `IMAGE_TAG` 与 `NODE_IMAGE` 示例项。
- 修复 `tests/auth-session.test.ts` 的日期相关失败：固定 2026-05-30 创建的 token 在 2026-06-06 后自然过期，测试已固定验证时间。
- 同步 `Readme.md`、`docs/TASKS.md`、`docs/AGENT_HANDOFF.md`、`docs/DEPLOYMENT.md`、`docs/TECH_PLAN.md`、`docs/QUALITY.md`、`docs/CONTEXT.md`、`docs/DECISIONS.md`。
- 同步 `.memory/context.md`、`.memory/knowledge.md`、`.memory/index.md`。
- 用户重新审批后 Docker Desktop daemon 可用，补跑 Compose 运行验证。
- `docker compose -p home-storage-m5-2-test up -d --build` 成功构建并启动测试容器。
- 通过 `/api/auth/setup` 创建测试管理员后，容器内 Prisma 查询 `users=1`。
- 强制重建容器后再次查询 `users=1`，验证 `home-storage-data` Volume 持久化生效。
- 测试 Compose 项目已执行 `docker compose -p home-storage-m5-2-test down -v` 清理。

## Verification

- `docker compose -p home-storage-m5-2-test config --quiet` 通过；本机 Docker config 权限警告不影响退出码。
- `AUTH_SECRET` 为空时，`docker compose -p home-storage-m5-2-test config --quiet` 按预期失败。
- `rg "APP_PORT" docker-compose.yml .env.example` 无命中。
- `npm run lint` 通过。
- `npm run typecheck` 通过。
- `npm run test -- tests/auth-session.test.ts` 通过。
- `npm run test` 通过：34 个测试文件、113 个测试。
- `npm run build` 通过。
- `npm run db:check` 通过。
- `docker compose -p home-storage-m5-2-test up -d --build` 通过。
- `GET http://localhost:3102/setup` 返回 200。
- `/api/auth/setup` 创建 `m5-compose-admin` 返回 201。
- 容器内 Prisma 查询 `users=1`。
- `docker compose -p home-storage-m5-2-test up -d --force-recreate --no-build` 后容器内 Prisma 再次查询 `users=1`。
- 重建后 `GET http://localhost:3102/login` 返回 200，日志显示 `SQLite schema already initialized.`。

## Blocker

- 当前无 M5.2 阻塞。
- 观察到生产容器 Cookie 带 `Secure`，纯 HTTP 脚本登录后不会回传 Cookie；M5.3 需明确局域网 HTTP/HTTPS、反向代理和 Cookie 安全策略。

## Next Step

进入 M5.3 初始化设置流程与生产环境配置校验，重点处理生产 `AUTH_SECRET`、HTTP/HTTPS Cookie 行为、首次管理员初始化和 LLM 默认配置读取边界。
