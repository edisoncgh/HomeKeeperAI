# Session 20260603-002847

## Summary
修复 M3.5 订单截图解析真实试用中的超时问题：用户在 `/settings` 测试 LLM API 成功，但 `/api/items/parse-order` 两次约 60 秒后返回 504。诊断确认健康检查只覆盖文本 chat/completions，订单截图解析是 vision 请求；默认 LLM client 30 秒超时 + 1 次重试会叠成约 60 秒失败。

## Changes
- 新增 `lib/api/ai-vision.ts`，定义视觉请求策略：`timeoutMs=120000`、`maxRetries=0`。
- 更新 `lib/api/ai-recognition.ts` 和 `lib/api/ai-order-parsing.ts`，调用 `createChatCompletion` 时复用视觉请求策略。
- 新增 `tests/ai-vision-request-policy.test.ts`，覆盖拍照识别和订单截图解析两个 vision 入口都传递长超时且禁用自动重试。
- 同步 `docs/TASKS.md`、`docs/API.md`、`docs/QUALITY.md`、`docs/AGENT_HANDOFF.md`。
- 同步 `.memory/context.md`、`.memory/knowledge.md`、`.memory/index.md`。

## Verification
- RED：`tests/ai-vision-request-policy.test.ts` 先失败，确认两个视觉入口未传递 `timeoutMs` 和 `maxRetries`。
- `npm run test -- tests/ai-vision-request-policy.test.ts` 通过，1 个测试文件、2 个测试。
- `npm run test -- tests/ai-recognition-api.test.ts tests/ai-order-parsing-api.test.ts tests/ai-vision-request-policy.test.ts` 通过，3 个测试文件、11 个测试。
- `npm run test` 通过，25 个测试文件、79 个测试。
- `npm run typecheck` 通过。
- `npm run lint` 通过。
- `npm run build` 通过。
- `npm run db:check` 通过。
- `rg "DEBUG-|console\.log|console\.debug" lib app components tests` 无匹配。

## Next
等待用户重启 dev server 后在 `/items` 重新测试真实订单截图解析。如果仍超时，下一步优先评估前端图片压缩/缩放或模型侧 vision 响应耗时；如果通过，再进入 M3.6 主页 AI 建议规划。

## Tags
`#m3` `#ai` `#order-parsing` `#diagnose` `#bugfix` `#tdd` `#verification` `#memory`
