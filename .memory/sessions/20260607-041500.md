# Session 20260607-041500

## Summary

将 `Readme.md` 从阶段状态摘要改写为正式仓库 README，补充项目定位、主要功能、技术栈、Docker Compose 快速部署、本地开发、环境变量、备份恢复、验证命令、1.0 发布候选人工验收清单和当前范围限制。

## Scope

- 更新 `Readme.md`，不修改业务代码。
- 明确 1.0 当前状态为“发布候选自动验证通过，等待人工验收和 Git 收口”。
- 补充生产部署关键说明：`AUTH_SECRET` 必填，纯 HTTP 局域网部署需要 `AUTH_COOKIE_SECURE=false`，HTTPS/反向代理保持 `true`。
- 补充 Docker Volume、SQLite 数据路径、备份恢复、LLM 设置和 Docker Hub 不可达时 `NODE_IMAGE` 兜底说明。

## Verification

- 快速扫描 README，确认包含 `AUTH_COOKIE_SECURE`、`AUTH_SECRET`、主要功能、快速部署、备份恢复、1.0 发布候选和未实现范围说明。
- 本次为文档更新，未运行代码测试。

## Notes

- README 现在可作为仓库首页给外部读者或后续用户快速理解项目，不再只依赖 `docs/` 和 `.memory` 才能知道如何部署。

## Tags

#readme #docs #m5 #release-candidate #deployment #memory
