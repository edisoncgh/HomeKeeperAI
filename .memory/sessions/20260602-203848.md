# Session 20260602-203848

## Summary

完成 M3.5 订单截图解析代码实现、自动验证、运行时入口验证、docs 同步和项目记忆更新。

## Changes

- `lib/ai/prompts.ts`：`buildOrderParsingPrompt` 注入现有分类/位置摘要，并加入订单 JSON 示例，明确 `source=order`、价格、采购日期和 0-1 置信度。
- 新增 `lib/api/ai-order-parsing.ts`：登录保护、订单截图 data URL 校验、分类/位置摘要读取、LLM vision 调用、候选解析和空候选 warnings 兜底。
- 新增 `app/api/items/parse-order/route.ts`。
- 新增 `components/ai/order-parsing-panel.tsx`，并在 `components/ai/index.ts` 导出。
- `components/inventory/item-manager.tsx` 接入订单解析面板；候选确认后继续复用 M3.3 写入和现有 `POST /api/items`。
- 新增 `tests/ai-order-parsing-api.test.ts`、`tests/ai-order-parsing-ui.test.ts`，扩展 `tests/ai-contract.test.ts`。
- 同步 `docs/TASKS.md`、`docs/API.md`、`docs/TECH_PLAN.md`、`docs/DESIGN_SYSTEM.md`、`docs/QUALITY.md`、`docs/CONTEXT.md`、`docs/AGENT_HANDOFF.md`、`Readme.md` 和 `.memory/`。

## Verification

- RED：新增 M3.5 API/UI/Prompt 测试时，订单解析 API helper、订单解析组件和订单 Prompt taxonomy 上下文缺失导致测试失败。
- `npm run test -- tests/ai-contract.test.ts tests/ai-order-parsing-api.test.ts tests/ai-order-parsing-ui.test.ts`：通过，3 个测试文件、9 个测试。
- `npm run test`：通过，24 个测试文件、77 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，新增 `/api/items/parse-order` 动态路由。
- `npm run db:check`：通过。
- 临时 SQLite + dev server 初始化管理员后访问 `/items` 返回 200，页面包含“AI 订单解析”“不会保存订单截图”“解析候选”。

## Remaining

- 用户需使用真实订单截图人工验收：多物品候选、价格、采购日期、数量、warnings 和逐项确认入库。
- 下一步建议进入 M3.6 主页 AI 建议规划；编码前仍需先输出切片计划并等待确认。
