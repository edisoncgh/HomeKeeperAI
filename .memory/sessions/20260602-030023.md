# Session 20260602-030023

## Summary

完成 M3.2 AI 结构化契约与解析校验，并修复 M3.1.5 复核发现的开发环境 LLM API Key 加密持久化隐患。

## User Request

用户确认 `/settings` 页面已添加 LLM API 且测试连接成功，要求先检查是否真的成功；若无问题则继续按规范开始 M3.2 开发。

## Work Completed

- 复核本地设置：`AppSetting` 中存在 `llm.baseUrl`、`llm.model`、`llm.apiKey`，说明设置页已保存配置。
- 发现开发环境未配置 `AUTH_SECRET` 时的隐患：旧实现只在当前进程中保存 fallback secret，用户当前 dev 进程测试连接可成功，但重启后新进程无法解密旧 API Key。
- 新增回归测试并修复：开发 fallback secret 持久化到 `data/dev-auth-secret`；测试可用 `AUTH_DEV_SECRET_PATH` 指定临时路径。
- 完成 M3.2：
  - `lib/ai/schemas.ts`：AI 候选字段来源、候选类型、JSON Schema。
  - `lib/ai/prompts.ts`：System Prompt、拍照识别 Prompt、订单截图 Prompt。
  - `lib/ai/parse.ts`：LLM 响应解析、JSON 恢复、字段校验、低置信度 warnings、无效候选恢复。
  - `tests/ai-contract.test.ts` 和 `tests/ai-candidate-parser.test.ts`：Prompt/Schema/解析回归样例。
- 同步 docs 和 `.memory` 到 M3.2 完成状态，下一步为 M3.3 候选确认 API/UI 边界规划。

## Verification

- `npm run test -- tests/ai-candidate-parser.test.ts tests/ai-contract.test.ts`：通过，2 个测试文件、9 个测试。
- `npm run test -- tests/auth-session.test.ts tests/settings-api.test.ts tests/settings-crypto.test.ts tests/ai-api.test.ts tests/ai-client.test.ts tests/ai-config.test.ts tests/ai-candidate-parser.test.ts tests/ai-contract.test.ts`：通过，8 个测试文件、29 个测试。
- `npm run test`：通过，18 个测试文件、57 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- `npm run db:check`：通过。

## Notes

- 用户在修复前保存的本地 LLM API Key 使用旧进程内 fallback secret 加密；新进程中不可解密。需要在 `/settings` 中重新替换保存一次 API Key，之后重启开发服务也可稳定。
- 本轮未实现真实拍照识别、订单解析、主页 AI 建议、候选确认 UI 或正式物品写入。

## Tags

#m3 #ai #prompt #schema #parser #settings #security #tdd #verification
