# Session: 2026-05-30 20:23

## Request

用户要求在进入 M1.2 前，将 `/docs`、`.CLAUDE`、`.AGENTS` 加入 `.gitignore`，并删除 Git 中这三个项目的记录。

## Context

- 仓库实际跟踪的是 `docs/`、`CLAUDE.md`、`AGENTS.md`。
- 未发现 `.CLAUDE`、`.AGENTS` 隐藏文件被 Git 跟踪。

## Analysis

- 为满足用户写法和实际仓库状态，`.gitignore` 同时加入 `/docs/`、`CLAUDE.md`、`AGENTS.md`、`.CLAUDE`、`.AGENTS`。
- 使用 `git rm -r --cached` 只移除 Git 索引记录，不删除本地文件。

## Actions

- 更新 `.gitignore`。
- 运行 `git rm -r --cached docs CLAUDE.md AGENTS.md`。
- 更新 `.memory/context.md`、`.memory/index.md`、`.memory/knowledge.md`。

## Outcome

`docs/`、`CLAUDE.md`、`AGENTS.md` 已从 Git 索引移除，本地文件仍保留。后续这些路径会被 Git 忽略。

## Tags

#git #ignore #docs

## Open Items

- 需要提交本次 Git 跟踪策略变更。
