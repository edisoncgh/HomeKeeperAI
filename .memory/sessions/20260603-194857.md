# Session 20260603-194857

## Summary
修复 M3.6 主页 AI 建议真实试用超时问题。用户反馈 `/settings` 测试连接成功，但首页“生成建议”请求约 62 秒后返回 504；诊断确认主页建议沿用了 LLM client 默认 30 秒超时和 1 次自动重试，导致慢速远端模型重复等待同一建议请求。

## Changes
- 新增 `tests/ai-dashboard-request-policy.test.ts`：回归覆盖主页建议调用 `createChatCompletion` 时必须传递长耗时交互式请求策略。
- 新增 `lib/api/ai-interactive.ts`：定义 `AI_INTERACTIVE_REQUEST_POLICY`，当前为 `timeoutMs=120000`、`maxRetries=0`。
- 更新 `lib/api/ai-dashboard-advice.ts`：调用 LLM 时传入交互式请求策略。
- 同步 `docs/TASKS.md`、`docs/API.md`、`docs/QUALITY.md`、`docs/AGENT_HANDOFF.md`、`docs/CONTEXT.md`、`Readme.md` 和 `.memory/`。

## Verification
- RED：新增 `tests/ai-dashboard-request-policy.test.ts` 后，先观察到主页建议未传递 `timeoutMs` 和 `maxRetries`。
- `npm run test -- tests/ai-dashboard-request-policy.test.ts` 通过，1 个测试文件、1 个测试。
- `npm run test -- tests/ai-dashboard-advice.test.ts tests/ai-dashboard-advice-api.test.ts tests/dashboard-home-ui.test.ts tests/ai-dashboard-request-policy.test.ts` 通过，4 个测试文件、9 个测试。
- `npm run test` 通过，29 个测试文件、88 个测试。
- `npm run typecheck` 通过。
- `npm run lint` 通过。
- `npm run build` 通过。
- `npm run db:check` 通过。
- `rg "DEBUG-|console\.log|console\.debug" lib app components tests` 未发现残留调试输出。

## Notes
- `/api/ai/health` 只代表轻量文本 chat/completions 连通性成功，不代表库存摘要建议、vision 识别或订单截图解析一定能在默认短超时时间内完成。
- M3.6 真实远端建议生成修复后仍需用户重新点击首页“生成建议”人工验收。

## Next
请用户在已配置 LLM 的本地环境中重新点击首页“生成建议”；确认通过后再进入 M3 收口与复查规划。

## Tags
`#m3` `#ai` `#dashboard-advice` `#diagnose` `#bugfix` `#tdd` `#verification` `#memory`
