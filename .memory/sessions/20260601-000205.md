# Session 20260601-000205

## Request

用户确认 M2.1 登录跳转 bug 已修复，要求继续遵照 `CLAUDE.md` 开发规范进入 M2.2。

## Work Completed

- 按 handoff 文档确认 M2.2 范围：物品 API、物品校验、列表查询、创建入库记录事务；不做物品 UI、AI、出库、预警或 Docker。
- 新增 `lib/validation/item.ts` 和 `tests/item-validation.test.ts`，覆盖物品输入和查询参数解析。
- 新增 `lib/api/items.ts`、`app/api/items/route.ts`、`app/api/items/[id]/route.ts`。
- 新增 `tests/item-api.test.ts`，覆盖未登录 401、创建物品写入 `ItemRecord(type=IN)`、缺失分类引用阻断写入。
- 更新 `docs/TASKS.md`、`docs/API.md`、`docs/TECH_PLAN.md`、`docs/QUALITY.md`、`docs/DATA_MODEL.md`、`docs/CONTEXT.md`、`docs/AGENT_HANDOFF.md` 和 `Readme.md`。

## Verification

- `npm run test`：通过，9 个测试文件、22 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- `npm run db:check`：通过。
- 临时 SQLite + dev server API 验证：未登录物品列表 401；初始化管理员 201；分类/位置创建 201；缺失分类引用 400；物品创建 201；详情 200 且记录为 `IN/2`；列表查询 200；更新 200；删除 200；删除后详情 404。

## Notes

- 首次 `npm run build` 因 `.next/trace` EPERM 失败；停止残留 Node worker、清理 `.next` 后重跑通过。
- Git 继续提示无法访问用户全局 ignore 文件，不影响提交。

## Next

进入 M2.3：实现 `/items` 物品管理 UI，复用现有 AppShell、基础 UI、分类/位置 API 和物品 API。
