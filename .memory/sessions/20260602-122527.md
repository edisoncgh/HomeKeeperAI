# Session 20260602-122527

## Summary

恢复新会话上下文，按 `CLAUDE.md`、`project-memory` 和 `handoff-driven-development` 读取项目记忆与 docs，并完成 M3.3 AI 候选确认 API/UI 边界切片规划。

## User Request

用户要求继续开发 `HomeStorageAssistant`，先严格遵循 `CLAUDE.md`，使用 `project-memory` 和 `handoff-driven-development`，读取 `.memory/`、核心 docs 和 M3 相关文档。当前进入 M3.3，但编码前必须先输出切片计划并等待用户确认。

## Work Completed

- 读取 `CLAUDE.md`、`.memory/context.md`、`.memory/knowledge.md`、`.memory/index.md`。
- 读取 `docs/AGENT_HANDOFF.md`、`docs/TASKS.md`、`docs/TECH_PLAN.md`、`docs/CONTEXT.md`、`docs/API.md`、`docs/DESIGN_SYSTEM.md`、`docs/QUALITY.md`。
- 读取 M3 相关 `docs/PRD.md`、`docs/DOMAIN.md`、`docs/DATA_MODEL.md`、`docs/DECISIONS.md`、`docs/DEPLOYMENT.md`。
- 复核现有代码边界：`lib/ai/schemas.ts`、`lib/ai/parse.ts`、`lib/api/items.ts`、`lib/inventory/item-view.ts`、`lib/validation/item.ts`、`app/items/page.tsx`。
- 更新 `docs/TASKS.md`：补齐 M3.3 范围、非目标、验收标准和建议实现切片；修正底部 M3.2 勾选状态。
- 更新 `docs/AGENT_HANDOFF.md`：记录 M3.3 当前等待用户确认，以及推荐实现顺序。
- 更新 `.memory/context.md`：记录当前状态为 M3.3 已规划、等待用户确认。

## Planned M3.3 Slices

- M3.3.1 候选确认纯逻辑：将 `AiItemCandidate` 转成前端可编辑表单状态，保留来源、置信度、推断说明和 warnings，并测试 `source=user` 语义。
- M3.3.2 候选确认 UI：新增可复用确认面板，展示字段来源、置信度和 warnings，允许用户修改字段并逐项确认。
- M3.3.3 收口验证：确认写入复用现有 `POST /api/items`，同步 docs 和 `.memory`，运行完整验证。

## Verification

本轮只做规划文档和项目记忆更新，未修改业务代码，未运行代码级验证。

## Notes

- M3.3 不直接实现真实拍照识别、订单截图解析、主页 AI 建议或图片上传。
- M3.3 不新增 AI 候选持久化表，不自动创建分类、位置或标签。
- 用户确认计划前不要开始编码。
- 本地旧加密 LLM API Key 仍可能需要用户在 `/settings` 中重新替换保存一次。

## Tags

#m3 #ai #planning #handoff #memory
